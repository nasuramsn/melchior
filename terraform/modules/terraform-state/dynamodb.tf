resource "aws_dynamodb_table" "terraform" {
  name         = "rs-terraform-${var.environment}"
  billing_mode = "PAY_PER_REQUEST"
  hash_key     = "LockID"

  attribute {
    name = "LockID"
    type = "S"
  }
}